<h1>Cocktails#index</h1>
<p>Find me in app/views/cocktails/index.html.erb</p>

<% @cocktails.each do |cocktail| %>
  <p><%= link_to cocktail.name, cocktail_path(cocktail) %></p>
  <p><%= link_to "Add dose to #{cocktail.name}", new_cocktail_dose_path(cocktail) %></p>
  <p><%= link_to "Remove cocktail", cocktail_path(cocktail), method: :delete %></p>
<% end %>

<p><%= link_to "New ingredient", new_ingredient_path %></p>
<%= link_to "New cocktail", new_cocktail_path %>
